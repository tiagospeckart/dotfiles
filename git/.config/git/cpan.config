[user]
    email = waterkip@cpan.org

[bum]
    upstream = upstream
    upstream = origin
    issueprefix = GL
    issueformat = GL-[0-9]+

[bum "branches"]
    protected = master
    protected = devel
