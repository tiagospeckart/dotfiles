[user]
    email = wesley@opndev.io

[bum]
    upstream = upstream
    upstream = origin
    issueprefix = OPNDEV
    issueformat = OPNDEV-[0-9]+

[bum "branches"]
    protected = master
    protected = devel
